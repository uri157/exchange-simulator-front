# Base HTTP para requests REST (sin trailing slash).
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# Base WS opcional. Si no se define, se deduce dinámicamente según el origin o API_BASE_URL.
# Formato: ws://host:port o wss://host:port (sin trailing slash).
#NEXT_PUBLIC_WS_BASE_URL=ws://localhost:3001

# Path del endpoint WS (con leading slash). Backend actual expone /ws
NEXT_PUBLIC_WS_PATH=/ws
